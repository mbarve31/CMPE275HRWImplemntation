package madhura.sjsu.cache.conf;

import com.yammer.dropwizard.config.Configuration;

/**
 * @author Madhura
 *
 *  This class extends the Configuration class which handles the configuration file(server_config.yml)
 *  and builds an instance of service’s configuration class by mapping YML field names to object field names.
 */

public class ConfigCacheService extends Configuration {

}
